# 设置
Settings:
  # 可选项目
  # VERBOSE   ->    详细
  # FINEST    ->    最好的
  # FINE      ->    精细
  # INFO      ->    信息
  # WARN      ->    警告
  # ERROR     ->    错误
  # FATAL     ->    致命
  # []        ->    默认【INFO】
  Logger: INFO
# 样式
Style:
  # 在动态调整输出后是否显示样例
  ExampleLogger: true